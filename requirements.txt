# Python dependencies for Systems From Scratch.
#
# Pinned deliberately: this book makes performance claims, and an unpinned dependency
# invalidates them silently (PLAN.md §12.4). Versions verified 2026-09-13.
#
# The book *builder* is not here — MyST comes from npm, pinned in package.json, so there is
# exactly one source of truth for its version.
#
# The list is one line long, and that is the point. The companion code is C; Python's only jobs
# here are to drive compilers, stamp JSON, and assemble the PDF. Percentiles come from the
# standard library's `statistics`, and the book's figures are deterministic SVG written by
# scripts/render-figures.py rather than by a plotting library — see AUTHORING_GUIDE.md for why
# a plot that is not byte-reproducible cannot be checked for staleness. A pin nothing imports is
# a version claim nobody checks.

# --- reads myst.yml for the chapter order (scripts/build-pdf.py) ---
# The PDF itself is rendered by Chromium, which is not a Python package:
# `pip install playwright && playwright install chromium`, or any system Chrome.
PyYAML==6.0.1
